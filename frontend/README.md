
  # Login system integration

  This is a code bundle for Login system integration. The original project is available at https://www.figma.com/design/vpOFdoia7f2x1zBHBgikcL/Login-system-integration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  